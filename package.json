{
  "name": "process_cucumber_output",
  "version": "1.0.0",
  "description": "process cucumber 'cucumber_report.json' file to list passed tests",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs": "^0.0.1-security"
  }
}
